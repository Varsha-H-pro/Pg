{
  "name": "pg-perfect",
  "version": "1.0.0",
  "description": "PG Perfect Management System - Full Stack Application",
  "scripts": {
    "setup": "chmod +x setup.sh && ./setup.sh",
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend/perfect-pg-front && npm run start",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend/perfect-pg-front && npm install"
  },
  "keywords": ["pg-management", "nodejs", "react", "mongodb"],
  "author": "PG Perfect Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}